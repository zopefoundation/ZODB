language: python
sudo: false
python:
    - pypy
    - pypy3
    - 2.7
    - 3.3
    - 3.4
    - 3.5
install:
    - pip install zc.buildout
    - buildout
script:
    - bin/test -v1j99
    - if [[ $TRAVIS_PYTHON_VERSION != 'pypy3' ]]; then cd doc; make html; fi
notifications:
    email: false
cache:
  directories:
    - eggs
